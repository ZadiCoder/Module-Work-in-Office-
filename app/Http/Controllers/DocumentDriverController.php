<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use App\Models\Document_Driver;
class DocumentDriverController extends Controller
{
    public function provider_create_work(Request $request){
        //dd($request->input());
        // $driver = new Document_Driver;
        // $driver->image
        // $driver->save();
        // redirect()->route('provider-index');
    }
}
